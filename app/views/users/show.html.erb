<h1><%=t 'session.login' %>: <%= @user.login %></h1>

<p><%=t 'users.registered_on' %>: <%=l @user.created_at.to_date, :format => :long %> </p>
<p><%=t 'users.total_bookmarks' %>: <%= @user.bookmarks.size %></p>
<p><%=t 'users.total_tags' %>: <%= @user.bookmarks.tag_counts.size %></p>

<%= link_to t('users.edit_profile'), edit_user_path(@user) %><br />
<%= link_to t('common.home'), bookmark_sites_path %>
